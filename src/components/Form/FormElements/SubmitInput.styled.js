import styled from 'styled-components';

export const SubmitInputWrapper = styled.input`
  border: none;
  background-color: #f2877d;
  color: #fff;
  padding: 5px 25px;
  font-size: 16px;

  cursor: pointer;

  box-shadow: 0 0 3px 1px rgba(0, 0, 0, 0.3);

  :hover {
    transform: scale(0.95);
  }
`;
